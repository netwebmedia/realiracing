name: Deploy to realiracing.com

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-realiracing
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify photo attribution
        run: node _ci/validate-photo-attribution.mjs

      # Suffixed twins (<slug>-2) and an exhausted topic pool. The generator
      # used to auto-suffix a slug collision while TOPIC_POOL wrapped around to
      # covered ground, so the two faults compounded: guaranteed collisions,
      # each shipped as a near-identical second page competing with the first
      # for the queries that earn the commission.
      - name: Validate blog queue
        run: node _ci/validate-blog-queue.mjs

      # Which reply shapes the generator must turn into posts. A bare object —
      # what the model returns for a single-post batch about a third of the time
      # — was being discarded as unparseable, losing 11 already-written posts in
      # the 2026-09-16 run while the run still reported 20/20.
      - name: Validate batch parse contract
        run: node _ci/validate-batch-parse.mjs

      - name: Preflight — verify secrets
        id: preflight
        run: |
          if [ -z "${{ secrets.FTP_HOST }}" ] || [ -z "${{ secrets.FTP_USER }}" ] || [ -z "${{ secrets.FTP_PASSWORD }}" ]; then
            echo "::error::FTP secrets not configured. Add FTP_HOST, FTP_USER, FTP_PASSWORD in repo Settings → Secrets → Actions."
            exit 1
          fi
          echo "Secrets present — proceeding."

      - name: Stage files
        run: |
          mkdir -p _stage
          # Copy everything except dev/build artifacts
          rsync -a --exclude='.git' --exclude='.github' --exclude='node_modules' \
            --exclude='.DS_Store' --exclude='Thumbs.db' --exclude='remotion-highlights' \
            --exclude='extract-slides.html' --exclude='_stage' \
            --exclude='_deploy' \
            . _stage/
          echo "── Stage contents ──"
          ls -1 _stage

      - name: Regenerate CSP inline-script hashes
        run: |
          set -euo pipefail
          # The enforced CSP has no 'unsafe-inline' in script-src — every inline
          # <script> is allowed by sha256 instead. The blog machine publishes
          # posts with their own inline gear-box call, so the list is rebuilt
          # from the staged HTML rather than trusted from the repo. Without this
          # the first auto-published post would be silently refused by the
          # browser and its gear box would render empty on a green deploy.
          node _deploy/csp-inline-hashes.js _stage
          grep -o "script-src[^;]*" _stage/.htaccess | tail -1 | cut -c1-120

      - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./_stage/
          server-dir: ./
          dangerous-clean-slate: false
          log-level: standard
          exclude: |
            **/.git*
            **/.github/**
            **/.DS_Store
            **/Thumbs.db
            **/node_modules/**
            **/remotion-highlights/**
            **/extract-slides.html
            **/_deploy/**

      - name: Smoke test
        run: |
          code=$(curl -sSL -o /dev/null -w "%{http_code}" \
            -A "Mozilla/5.0 (gh-actions smoke test)" --max-time 20 \
            "https://realiracing.com/")
          echo "Homepage: $code"
          [ "$code" = "200" ] || { echo "Non-200 response — check the site"; exit 1; }
          echo "realiracing.com is live"
