/* RealIRacing — gear.html bootstrap.
   Extracted from an inline <script> on 2026-08-18 so script-src can stay strict
   (no 'unsafe-inline'). Loads after /js/affiliates.js in document order, exactly
   as the inline block did, so RIR_AFF and renderGearBox are already defined. */
RIR_AFF.hydrateLinks();
renderGearBox('rir-gear-reco', ['cockpit', 'meta-quest-3'], 'Worth a look before your next upgrade');
